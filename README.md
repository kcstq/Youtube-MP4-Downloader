# Youtube-MP4-Downloader
A simple python script to download YouTube Videos in MP4 Format


This script uses the yt-dlp library for python.
Furthermore it uses H.264 instead of YouTube's AV1 format because otherwise the downloaded video can't be opened via QuickTime Player on MacOS.
